@model IEnumerable<NguyenMinhHiep220900100project2.Models.NguoiDung>

@{
    ViewBag.Title = "Danh Sách Người Dùng";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">@ViewBag.Title</h2>

    <div class="d-flex justify-content-between align-items-center mb-3">
        <p class="lead">Danh sách người dùng trong hệ thống.</p>
        @Html.ActionLink("Tạo Người Dùng Mới", "Create", null, new { @class = "btn btn-primary" })
    </div>

    <table class="table table-striped table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.TenDangNhap)</th>
                <th>@Html.DisplayNameFor(model => model.MatKhauHash)</th>
                <th>@Html.DisplayNameFor(model => model.VaiTro)</th>
                <th>@Html.DisplayNameFor(model => model.NgayTao)</th>
                <th>@Html.DisplayNameFor(model => model.NgayCapNhat)</th>
                <th class="text-center">Thao Tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.TenDangNhap)</td>
                    <td class="text-truncate" style="max-width: 150px;">@Html.DisplayFor(modelItem => item.MatKhauHash)</td>
                    <td>@Html.DisplayFor(modelItem => item.VaiTro)</td>
                    <td>@Html.DisplayFor(modelItem => item.NgayTao)</td>
                    <td>@Html.DisplayFor(modelItem => item.NgayCapNhat)</td>
                    <td class="text-center">
                        <div class="btn-group" role="group">
                            @Html.ActionLink("Sửa", "Edit", new { id = item.MaNguoiDung }, new { @class = "btn btn-sm btn-warning" })
                            @Html.ActionLink("Chi Tiết", "Details", new { id = item.MaNguoiDung }, new { @class = "btn btn-sm btn-info" })
                            @Html.ActionLink("Xóa", "Delete", new { id = item.MaNguoiDung }, new { @class = "btn btn-sm btn-danger" })
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
